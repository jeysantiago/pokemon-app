import { DecimetersToFeetPipe } from './decimeters-to-feet.pipe';

describe('DecimetersToFeetPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimetersToFeetPipe();
    expect(pipe).toBeTruthy();
  });
});
