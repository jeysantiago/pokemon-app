import { HectogramsToKgPipe } from './hectograms-to-kg.pipe';

describe('HectogramsToKgPipe', () => {
  it('create an instance', () => {
    const pipe = new HectogramsToKgPipe();
    expect(pipe).toBeTruthy();
  });
});
