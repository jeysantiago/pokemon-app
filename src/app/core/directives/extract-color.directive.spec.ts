import { ExtractColorDirective } from './extract-color.directive';

describe('ExtractColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtractColorDirective();
    expect(directive).toBeTruthy();
  });
});
